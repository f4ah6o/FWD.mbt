# 2026-05-26 開発日誌

## 今日の計画

1. v6 workbench の `purchase-request` preset を shadow 運用の deterministic demo として固定する。
2. HTML / MX / API error の contract を fixture と smoke check で確認できるようにする。
3. all-target の対象を core/API/view に限定し、browser client は JS-only として CI 境界を明確にする。
4. GitHub Actions の古い MoonBit archive 依存と `scripts/ci.sh` の壊れた schema path を直す。

## 実装メモ

- `purchase-request` は `Released + shadow` を初期値にしたまま、system of record は既存の人間・外部 system に残す。
- FWD は mirror された入力を validate / preview し、timeline / metrics / observability を見ながら drift を確認する入口として扱う。
- all-target は `src/workbench_v6`, `src/ui/workbench_v6`, `src/api_v6` に限定する。`src/ui/client` は DOM と mhx FFI に依存するため JS-only の smoke harness とする。

## 検証予定

- `moon check --target all src/workbench_v6 src/ui/workbench_v6 src/api_v6`
- `moon build src/ui/client/main --target js`
- `moon run src/workbench_shadow_ci`
- `just ci`
